<section class="auth-shell">
  <article class="auth-card">
    <div class="auth-copy">
      <p class="eyebrow">Sign in</p>
      <h1>Welcome back to Muse.</h1>
      <p>Pick up your library, reconnect with your circles, and keep listening without losing your place.</p>
    </div>

    <?php if(!empty($error)): ?>
    <p class="auth-error" role="alert"><?= htmlspecialchars($error, ENT_QUOTES, 'UTF-8') ?></p>
    <?php endif; ?>

    <form class="auth-form" action="/sign-in" method="post">
      <label>
        <span>Email address</span>
        <input name="email" type="email" autocomplete="email" placeholder="you@example.com" value="<?= htmlspecialchars($email ?? '', ENT_QUOTES, 'UTF-8') ?>" required />
      </label>

      <label>
        <span>Password</span>
        <input name="password" type="password" autocomplete="current-password" placeholder="Enter your password" required />
      </label>

      <button type="submit">Sign In</button>
    </form>

    <p class="auth-footnote">
      Need an account? Sign up from the landing page to create one.
    </p>
  </article>
</section>