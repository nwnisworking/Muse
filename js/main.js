import App from './App.js'
import ControllerRegistry from './ControllerRegistry.js'
import controllersList from './Controllers/index.js'

const registry = new ControllerRegistry()


// controllerList.forEach()

// controllers.forEach(module => registry.register(module.default))

// const app = new App({
//   loadingTemplate: document.getElementById('loading-template'),
//   root: document.querySelector('.app'),
//   controllerRegistry: registry
// })

// app.load('default')
